version: "3.9"

services:
  backend:
    build: ./backend
    container_name: go-backend
    ports:
      - "8080:8080"

  frontend:
    build: ./frontend
    container_name: astro-frontend
    ports:
      - "4321:4321"
    depends_on:
      - backend
